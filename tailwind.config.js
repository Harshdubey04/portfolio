/** @type {import('tailwindcss').Config} */
export default {
  // Use the `dark` class on <html> for theme switching
  darkMode: 'class',
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
};

